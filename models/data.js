require('dotenv').config()

const tasks = ["practice violin", "study", "schedule Mia's appointment", "call dad", "go to the gym", "work on resume"]:
const complete = ["finish cooking", "finish cleaning"];

module.exports = {tasks, complete}